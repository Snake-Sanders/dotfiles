# copy this file to $HOME directory and set the kbd file for your keybord.
echo "changing keyboard layout to Home Row Mode"

# selecte the configuration to load
# sudo kanata --quite --cfg ~/dotfiles/.config/kanata/macbookpro14.kbd
