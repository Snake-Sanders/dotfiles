# Markdown

## Web Preview

In Neovim type `:MarkdownPreview` to open a web preview.

## Selection

Use `vip` + `y` to yank only the text avoiding the delimiters ``.

## Table of content

"`[[_TOC_]]`"
